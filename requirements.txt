# 模块1 依赖 (PyTorch)
# 你的环境已经有大部分包了，只需确认以下包

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
seaborn>=0.11.0
scikit-learn>=1.0.0
tqdm>=4.60.0
Pillow>=8.0.0

# 可选：GPU加速需要CUDA
# torch+cu121 (你已有)